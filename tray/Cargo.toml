[package]
name = "trust-sentinel-tray"
version = "0.1.0"
edition = "2021"

[dependencies]
tray-icon = "0.19"
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"